{
    "name": "turnip-bot",
    "scripts": {
        "deploy:bot": "pulumi up --replace \"urn:pulumi:prod::turnip-bot::awsx:x:ecs:FargateService\\$aws:ecs/service:Service::turnip-bot\""
    },
    "devDependencies": {
        "@types/node": "^10.0.0"
    },
    "dependencies": {
        "@pulumi/aws": "^2.0.0",
        "@pulumi/awsx": "^0.18.10",
        "@pulumi/postgresql": "^2.2.1",
        "@pulumi/pulumi": "^2.0.0",
        "pcloudinit": "^0.1.0"
    }
}
